# CLAUDE.md

## Development Commands

### Installation and Setup
```bash
# Development installation with uv (recommended)
uv sync --extra dev
```

`uv sync` automatically rebuilds the Rust extension (`its_hub._rust`) whenever `.rs` sources or `Cargo.toml` change, thanks to the `[tool.uv] cache-keys` config in `pyproject.toml`. No separate `maturin develop` step is needed during normal development.

### Contribution
When commit or raising PR, never mention it is by ClaudeCode.
never say 🤖 Generated with [Claude Code](https://claude.ai/code)" in the commit statment, don't mention claude!

```bash
# Create commits with sign-off
git commit -s -m "commit message"

# For any git commits, always use the sign-off flag (-s)
```

### Testing
```bash
# Run tests
uv run pytest tests/ --ignore=tests/e2e
```
`tests/e2e/` is excluded because it requires a running OpenAI-compatible model server (see `tests/e2e/README.md`).

### Code Quality
```bash
# Run linter checks
uv run ruff check its_hub/

# Fix auto-fixable linting issues
uv run ruff check its_hub/ --fix

# Format code with ruff
uv run ruff format its_hub/
```

### IaaS Service (Inference-as-a-Service)
```bash
# Start IaaS service
uv run its-iaas --host 127.0.0.1 --port 8109

# Check service health
curl -s http://localhost:8109/v1/models | jq .

# For comprehensive IaaS setup (multi-GPU, reward models, etc.), see docs/iaas-service.md
```

## Additional Tips
- Use `rg` in favor of `grep` whenever it's available
- Use `uv` for Python environment management: always start with `uv sync --extra dev` to init the env and run stuff with `uv run`

## Coding Agent Plugin

This repo serves as a plugin for Claude Code and Codex CLI.

Both plugin manifests point to `./.claude/skills` for discovery.

### Plugin Config

User config is stored at `.its-hub/config.json` (auto-generated by the `setup-guide` skill, gitignored). API keys are **not** stored in the config — they are read from environment variables (`OPENAI_API_KEY`, `ANTHROPIC_API_KEY`, etc.).

### Plugin Development

- Skills are markdown files — edit `.claude/skills/*/SKILL.md` directly
- Discovery manifests rarely change
- Test plugin changes by invoking skills in Claude Code or Codex
<!-- LATTICE MANAGED BLOCK v0 — composed: Core → Language → Domain → Repo -->
<!-- Source: .lattice/packs/ — do not edit manually; changes will be overwritten -->
- [CORE-INS-001] (warn-only) Policy [escalation-boundary]: Agent must escalate when modifying public API signatures, deleting files, or changing security-critical code.
- [CORE-INS-002] (warn-only) Policy [forbidden-zones]: Agent must not modify CI workflows, toolchain pins/lockfiles, SPEC.md, or release/version files without explicit permission.
- [CORE-INS-003] (warn-only) Policy [test-weakening-policy]: Tests may only be weakened with human sign-off and a rationale explaining why the test is no longer valid.
- [CORE-INS-004] (warn-only) Policy [definition-of-done]: All tests pass, linting passes, touched docs updated in the same PR, and change is reviewed.
- [CORE-INS-005] (warn-only) Policy [change-size-budget]: Prefer small, focused changes. Split PRs exceeding ~400 changed lines or ~10 files.
- [CORE-INS-006] (warn-only) Policy [test-discipline]: All new code must have tests. Target 100% coverage. Failing test first for bugfixes.
- [CORE-INS-007] (warn-only) Policy [dependency-policy]: New dependencies require justification. Prefer stdlib and well-maintained packages.
- [CORE-INS-008] (warn-only) Policy [change-description-conventions]: Descriptive commit messages explaining the why, not the what. Conventional commit format.
- [CORE-INS-009] (warn-only) Policy [per-task-resource-cap]: 60 minutes or ~$10, whichever first. Then checkpoint: commit WIP, write status, wait.
- [CORE-INS-010] (warn-only) Policy [mandatory-review-triggers]: Changes to security-critical code, public APIs, and configuration files require human review.
- [PYTHON-INS-001] (warn-only) Language [python]: Use double-quoted strings consistently.
- [PYTHON-INS-002] (warn-only) Language [python]: Import ordering follows isort; its_hub is first-party.
- [PYTHON-INS-003] (warn-only) Language [python]: Line length convention is 88 characters.
- [PYTHON-INS-004] (warn-only) Language [python]: Prefer async patterns for I/O-bound operations.
- [RUST-INS-001] (warn-only) Language [rust]: Use Rust edition 2024.
- [RUST-INS-002] (warn-only) Language [rust]: Unsafe code is denied in all crate roots; unsafe belongs upstream.
- [RUST-INS-003] (warn-only) Language [rust]: Errors via thiserror, logging via tracing; no println/eprintln/dbg in production.
- [RUST-INS-004] (warn-only) Language [rust]: Do not hold sync guards across .await points.
- [RUST-INS-005] (warn-only) Language [rust]: No unwrap/expect/panic/indexing_slicing in production code.
- [RUST-INS-006] (warn-only) Language [rust]: Use #[expect(lint, reason)] instead of #[allow(lint)]; every suppression needs a reason.
- [RUST-INS-007] (warn-only) Language [rust]: Use workspace dependencies for version consistency; keep deps light.
- [RUST-INS-008] (warn-only) Language [rust]: All public and private items must have documentation.
- [RUST-INS-009] (warn-only) Language [rust]: Import traits with `as _` when only using methods; no glob imports.
- [RUST-INS-010] (warn-only) Language [rust]: No todo!/unimplemented!/exit; no mem::forget; no thread::sleep in async.
- [RUST-INS-011] (warn-only) Language [rust]: Comments answer "why?", never "what?"; runtime narration belongs in tracing.
- [RUST-INS-012] (warn-only) Language [rust]: Release profile: LTO, single codegen-unit, overflow-checks, panic=abort, strip.
- [RUST-INS-013] (warn-only) Language [rust]: Function limits: 30 lines, 25 cognitive complexity, 5 params, 2 bool params.
- [RUST-INS-014] (warn-only) Language [rust]: All commits must be cryptographically signed with DCO sign-off.
- [RUST-INS-015] (warn-only) Language [rust]: PR size limit 750 added lines (excludes Cargo, tests, docs, examples).
- [ITS-INS-001] (warn-only) Domain [its]: Public interfaces in api/; implementations in core/.
- [ITS-INS-002] (warn-only) Domain [its]: Algorithms must implement ainfer()/infer() interface.
- [ITS-INS-003] (warn-only) Domain [its]: Budget parameter must be a positive integer.
- [ITS-INS-004] (warn-only) Domain [its]: LM classes must implement async agenerate() methods.
- [ITS-INS-005] (warn-only) Domain [its]: LM resources require explicit cleanup (close() or async with).
- [ITS-INS-006] (warn-only) Domain [its]: Use StepGeneration for incremental reasoning tasks.
- [ITS-INS-007] (warn-only) Domain [its]: Cancelled async tasks must be awaited before returning.
- [ITS-INS-008] (warn-only) Domain [its]: Algorithm results must include token usage tracking (GenerationUsage).
- [ITS-INS-009] (warn-only) Domain [its]: Algorithms must handle partial LM failures gracefully — proceed with successful subset.
- [ITS-INS-010] (warn-only) Domain [its]: New algorithm variants should inherit from their base algorithm class.
- [ITS-INS-011] (warn-only) Domain [its]: All concurrent LM calls must go through the orchestrator.
- [ITS-INS-012] (warn-only) Domain [its]: Dependencies must only be imported from their declared extras group.
- [PRAXIS-INS-001] (warn-only) Domain [praxis]: Filter-first architecture; every behavior is a filter using HttpFilter/TcpFilter traits.
- [PRAXIS-INS-002] (warn-only) Domain [praxis]: Crate responsibility separation: praxis, praxis-core, praxis-filter, praxis-protocol, praxis-tls.
- [PRAXIS-INS-003] (warn-only) Domain [praxis]: Use #[serde(deny_unknown_fields)] on config structs; enums over strings; make invalid states unrepresentable.
- [PRAXIS-INS-004] (warn-only) Domain [praxis]: New filters follow 8-step checklist: trait impl, factory, registry, tests, example, integration test, filter docs, README sync.
- [PRAXIS-INS-005] (warn-only) Domain [praxis]: New capabilities require unit tests, integration tests, example config, example integration test, benchmarks.
- [PRAXIS-INS-006] (warn-only) Domain [praxis]: Sensitive values use SecretString (zeroize); expose only at injection boundary.
- [PRAXIS-INS-007] (warn-only) Domain [praxis]: Config validation at parse time, not runtime; wire validation into startup path.
- [PRAXIS-INS-008] (warn-only) Domain [praxis]: Alphabetical ordering for derive items, serde params; file ordering: constants, public, private, helpers, tests.
- [PRAXIS-INS-009] (warn-only) Domain [praxis]: Protocol implementations must cite governing RFCs with reference-style rustdoc links.
- [PRAXIS-INS-010] (warn-only) Domain [praxis]: Dynamic config reload must not disrupt in-flight requests; use ArcSwap atomic swap.
- [PRAXIS-INS-011] (warn-only) Domain [praxis]: No AI tool authorship in commits; CI rejects AI-authored or AI-signed commits.
- [PRAXIS-INS-012] (warn-only) Domain [praxis]: Small, composable, single-purpose functions; minimize side effects; prefer pure transformations.
- [REPO-INS-001] (warn-only) Repo: All commits must use sign-off (-s flag).
- [REPO-INS-002] (warn-only) Repo: Never mention AI generation in commits or PRs.
- [REPO-INS-003] (warn-only) Repo: Use uv for package management; run tools via uv run.
- [REPO-INS-004] (warn-only) Repo: Generated files (_version.py, proto/) must not be committed.
- [REPO-INS-005] (warn-only) Repo: Lint/format scope is its_hub/ only, not tests/scripts.
- [REPO-INS-006] (warn-only) Repo: Coverage target 100% for new code.
<!-- END LATTICE MANAGED BLOCK -->
