apiVersion: v2
name: its-hub
description: ITS inference-time scaling HTTP microservice
type: application
# Chart version — bump on chart changes.
version: 0.1.0
# Fallback for local `helm package`; the Helm publish workflow overrides this
# with `--app-version` computed from setuptools_scm (git tags).
appVersion: "1.2.0"
kubeVersion: ">=1.26.0-0"
keywords:
  - llm
  - inference-time-scaling
  - openai
  - gateway
  - openshift
home: https://github.com/Red-Hat-AI-Innovation-Team/its_hub
sources:
  - https://github.com/Red-Hat-AI-Innovation-Team/its_hub
